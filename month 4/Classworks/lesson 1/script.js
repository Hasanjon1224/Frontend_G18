const array = ["banana", "apple", "orange", "dinner"];

const result = document.getElementById("royxat");

    
for (let i = 0; i < array.length; i++) {
  result.innerHTML += `<li>${array[i]}</li>`;
   
}
